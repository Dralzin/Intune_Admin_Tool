<UserControl x:Class="IntuneAdminTool.Views.AppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="300" Padding="5" VerticalContentAlignment="Center"/>
            <ComboBox ItemsSource="{Binding OsFilterOptions}" SelectedItem="{Binding SelectedOsFilter}" Width="130" Margin="5,0" Padding="5" VerticalContentAlignment="Center"/>
            <ComboBox ItemsSource="{Binding TypeFilterOptions}" SelectedItem="{Binding SelectedTypeFilter}" Width="180" Margin="5,0" Padding="5" VerticalContentAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Loading applications..." Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" HorizontalAlignment="Center" Margin="0,10"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Apps}" SelectedItem="{Binding SelectedApp}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="250"/>
                <DataGridTextColumn Header="Type" Binding="{Binding AppType}" Width="130"/>
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="150"/>
                <DataGridTextColumn Header="Assigned" Binding="{Binding IsAssigned}" Width="80"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedDateTime, StringFormat=d}" Width="120"/>
                <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModifiedDateTime, StringFormat=d}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <GroupBox Grid.Row="2" Header="Application Details" Margin="10" Visibility="{Binding SelectedApp, Converter={StaticResource NullToCollapsed}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <!-- Logo and basic info -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Image Source="{Binding AppLogo}" Width="64" Height="64" Margin="0,0,16,0"
                               Stretch="Uniform" VerticalAlignment="Top"/>
                        <StackPanel DataContext="{Binding SelectedApp}">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Publisher, StringFormat='Publisher: {0}'}" Margin="0,2"/>
                            <TextBlock Text="{Binding PublishingState, StringFormat='State: {0}'}" Margin="0,2"/>
                            <TextBlock Text="{Binding CreatedDateTime, StringFormat='Created: {0:g}'}" Margin="0,2"/>
                            <TextBlock Text="{Binding IsAssigned, StringFormat='Assigned: {0}'}" Margin="0,2"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Description -->
                    <TextBlock Text="{Binding SelectedApp.Description}" TextWrapping="Wrap" Margin="0,0,0,12"
                               Foreground="#605E5C"/>

                    <!-- Assignments -->
                    <TextBlock Text="Assignments" FontWeight="SemiBold" Margin="0,8,0,4" Foreground="#0078D4"/>
                    <TextBlock Text="{Binding AssignmentsStatusMessage}" Margin="0,2,0,4" Foreground="#605E5C" FontStyle="Italic"
                               Visibility="{Binding AssignmentsStatusMessage, Converter={StaticResource NullToCollapsed}}"/>
                    <DataGrid ItemsSource="{Binding AppAssignments}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              MaxHeight="200" CanUserSortColumns="True"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              BorderThickness="1" BorderBrush="#EDEBE9">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Group" Binding="{Binding GroupDisplayName}" Width="300"/>
                            <DataGridTextColumn Header="Assignment Type" Binding="{Binding AssignmentType}" Width="120"/>
                            <DataGridTextColumn Header="Group ID" Binding="{Binding GroupId}" Width="280"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
