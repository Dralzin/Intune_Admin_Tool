<UserControl x:Class="IntuneAdminTool.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Report Selection -->
        <StackPanel Margin="10">
            <TextBlock Text="Select a Report" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding AvailableReports}" SelectedItem="{Binding SelectedReport}"
                          DisplayMemberPath="Name" Width="350" Padding="5" VerticalContentAlignment="Center"/>
                <Button Content="Run Report" Command="{Binding RunReportCommand}" Margin="8,0,0,0" Padding="16,6"
                        Style="{StaticResource PrimaryButtonStyle}"/>
                <Button Content="Cancel" Command="{Binding CancelReportCommand}" Margin="8,0,0,0" Padding="16,6"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>
                <Button Content="Export CSV" Command="{Binding ExportToCsvCommand}" Margin="8,0,0,0" Padding="16,6"/>
            </StackPanel>
            <TextBlock Text="{Binding SelectedReport.Description}" Margin="0,6,0,0" Foreground="#605E5C" FontStyle="Italic"
                       Visibility="{Binding SelectedReport, Converter={StaticResource NullToCollapsed}}"/>
        </StackPanel>

        <!-- Status -->
        <StackPanel Grid.Row="1" Margin="10,0,10,5">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#605E5C" FontStyle="Italic"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToCollapsed}}"/>
            <TextBlock Text="Running report..." Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>

        <!-- Report Data Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding ReportData}" AutoGenerateColumns="False" IsReadOnly="True"
                  Margin="10" CanUserSortColumns="True" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="#EDEBE9"
                  x:Name="ReportGrid">
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="6,3"/>
                </Style>
            </DataGrid.Resources>
        </DataGrid>

        <!-- Footer with row count -->
        <Border Grid.Row="3" Background="#F3F2F1" Padding="10,6">
            <TextBlock>
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} rows">
                        <Binding Path="ReportData.Count"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
